import { Josefin_Sans } from "next/font/google";

/** Marka alt sloganı: ince, sade, zarif */
export const brandTaglineFont = Josefin_Sans({
  weight: "200",
  subsets: ["latin", "latin-ext"],
  display: "swap",
  adjustFontFallback: true,
});
